
    import 'package:flutter/material.dart';
    class ExersicePage extends StatelessWidget {
        const ExersicePage({Key? key}) : super(key: key);
      
        @override
        Widget build(BuildContext context) {
          return Scaffold(
            appBar: AppBar(
              title: Text('Exersice Page'),
            ),
            );
            }
        }
    