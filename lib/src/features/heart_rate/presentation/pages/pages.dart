
    import 'package:flutter/material.dart';
    class Heart_ratePage extends StatelessWidget {
        const Heart_ratePage({Key? key}) : super(key: key);
      
        @override
        Widget build(BuildContext context) {
          return Scaffold(
            appBar: AppBar(
              title: Text('Heart_rate Page'),
            ),
            );
            }
        }
    