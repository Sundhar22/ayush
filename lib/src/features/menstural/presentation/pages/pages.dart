
    import 'package:flutter/material.dart';
    class MensturalPage extends StatelessWidget {
        const MensturalPage({Key? key}) : super(key: key);
      
        @override
        Widget build(BuildContext context) {
          return Scaffold(
            appBar: AppBar(
              title: Text('Menstural Page'),
            ),
            );
            }
        }
    