 
    import '../repositories/repositories.dart';

    class GetWatch_connectivityUseCase {
        final Watch_connectivityRepository repository;
      
        GetWatch_connectivityUseCase({required this.repository});
      
        // Future<User> execute(String userId) async {
        //   return userRepository.getUser(userId);
        // }
      }
      