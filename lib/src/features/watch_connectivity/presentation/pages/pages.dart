
    import 'package:flutter/material.dart';
    class Watch_connectivityPage extends StatelessWidget {
        const Watch_connectivityPage({Key? key}) : super(key: key);
      
        @override
        Widget build(BuildContext context) {
          return Scaffold(
            appBar: AppBar(
              title: Text('Watch_connectivity Page'),
            ),
            );
            }
        }
    